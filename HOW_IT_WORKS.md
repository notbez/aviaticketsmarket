# 🔄 Как работает обновление кода

## ❌ Автоматического обновления НЕТ

**Важно понимать:** Когда вы меняете код на вашем компьютере, он **НЕ обновляется автоматически** на сервере. Нужно вручную загружать изменения.

---

## 📋 ДВА РАЗНЫХ ПРОЕКТА

### 1. **Backend (tickets-backend)** - Сервер на Timeweb
- Это API сервер, который работает на сервере Timeweb
- Когда вы меняете код → нужно загружать на сервер вручную
- Работает постоянно на сервере

### 2. **Frontend (aviatickets-demo)** - Мобильное приложение
- Это приложение для телефона/планшета
- Когда вы меняете код → нужно пересобрать приложение
- Работает на устройстве пользователя

---

## 🔄 ПРОЦЕСС ОБНОВЛЕНИЯ BACKEND (на сервере)

### Когда вы меняете код в `tickets-backend/`:

1. **Внесите изменения** на вашем компьютере
2. **Загрузите файлы** на сервер (через SFTP или scp)
3. **На сервере выполните:**
   ```bash
   cd ~/aviatickets-backend
   npm install --production  # если добавили новые зависимости
   npm run build             # пересобрать проект
   pm2 restart aviatickets-backend  # перезапустить
   ```

### Пример:
```bash
# 1. На вашем компьютере изменили файл
# tickets-backend/src/booking/booking.service.ts

# 2. Загрузили на сервер через SFTP

# 3. На сервере:
cd ~/aviatickets-backend
npm run build
pm2 restart aviatickets-backend
```

---

## 📱 ПРОЦЕСС ОБНОВЛЕНИЯ FRONTEND (мобильное приложение)

### Когда вы меняете код в `aviatickets-demo/`:

1. **Внесите изменения** на вашем компьютере
2. **Перезапустите приложение:**
   ```bash
   cd aviatickets-demo
   npm start
   ```
3. **На телефоне** обновите приложение (Expo Go перезагрузит автоматически)

### Для финальной сборки (публикация в App Store/Google Play):
```bash
cd aviatickets-demo
eas build --platform ios     # для iOS
eas build --platform android # для Android
```

---

## 🌐 НА ЧТО МЕНЯТЬ API_BASE?

### Текущее значение:
```javascript
// aviatickets-demo/constants/api.js
export const API_BASE = 'https://airtickets-bcpu.onrender.com';
```

### Нужно заменить на адрес вашего сервера Timeweb:

#### Вариант 1: Если у вас есть домен
```javascript
export const API_BASE = 'https://ваш-домен.ru';
// Пример: 'https://api.mysite.ru'
```

#### Вариант 2: Если используете IP адрес
```javascript
export const API_BASE = 'http://ваш-ip-адрес:3000';
// Пример: 'http://123.45.67.89:3000'
```

#### Вариант 3: Если используете поддомен Timeweb
```javascript
export const API_BASE = 'https://ваш-поддомен.timeweb.ru';
// Пример: 'https://api123.timeweb.ru'
```

### ⚠️ ВАЖНО:
- Используйте `https://` если настроен SSL (рекомендуется)
- Используйте `http://` если SSL не настроен
- **НЕ добавляйте** `/api` или `/booking` в конце - это добавляется автоматически в коде

---

## 📝 ГДЕ ИСПОЛЬЗУЕТСЯ API_BASE?

API_BASE используется в следующих файлах:

1. ✅ **`constants/api.js`** - главный файл с константой
2. ✅ **`screens/HomeScreen.js`** - поиск рейсов
3. ✅ **`screens/SearchScreen.js`** - поиск (импортирует, но не использует напрямую)
4. ✅ **`screens/BookingScreen.js`** - создание бронирования (ИСПРАВЛЕНО)
5. ✅ **`screens/TicketsScreen.js`** - скачивание PDF билета (ИСПРАВЛЕНО)

**Все файлы теперь используют `API_BASE` из одного места!**

---

## 🔧 КАК ОБНОВИТЬ API_BASE

### Шаг 1: Откройте файл
```bash
# На вашем Mac
open /Users/vlasovmihail/Documents/aviatickets.nosync/aviatickets-demo/constants/api.js
```

Или в редакторе откройте:
```
aviatickets-demo/constants/api.js
```

### Шаг 2: Замените адрес
```javascript
// БЫЛО:
export const API_BASE = 'https://airtickets-bcpu.onrender.com';

// СТАНЕТ (пример):
export const API_BASE = 'https://api.mysite.ru';
// или
export const API_BASE = 'http://123.45.67.89:3000';
```

### Шаг 3: Сохраните файл

### Шаг 4: Перезапустите мобильное приложение
```bash
cd aviatickets-demo
npm start
```

---

## ✅ ПРОВЕРКА

После обновления API_BASE проверьте:

1. **Откройте мобильное приложение**
2. **Попробуйте найти рейс** - должно работать
3. **Попробуйте создать бронирование** - должно работать
4. **Попробуйте скачать PDF** - должно работать

Если что-то не работает:
- Проверьте, что сервер запущен: `pm2 status` на сервере
- Проверьте логи: `pm2 logs aviatickets-backend`
- Проверьте, что адрес правильный (без `/api` в конце)

---

## 📊 СХЕМА РАБОТЫ

```
┌─────────────────┐
│  Ваш компьютер  │
│                 │
│  tickets-backend│ ──[загрузить]──> ┌──────────────┐
│  (изменения)    │                  │   Сервер     │
└─────────────────┘                  │  Timeweb     │
                                     │              │
┌─────────────────┐                  │  tickets-    │
│  Ваш телефон    │                  │  backend    │
│                 │                  │  (работает)  │
│  aviatickets-   │ <──[API запросы]─└──────────────┘
│  demo           │
│  (приложение)   │
└─────────────────┘
```

---

## 🎯 КРАТКАЯ ПАМЯТКА

### Backend (сервер):
- Изменили код → Загрузить на сервер → `npm run build` → `pm2 restart`

### Frontend (приложение):
- Изменили код → Перезапустить приложение → Проверить на телефоне

### API_BASE:
- Менять только в `constants/api.js`
- Использовать адрес вашего сервера Timeweb
- Без `/api` в конце

